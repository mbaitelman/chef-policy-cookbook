job('UploadPolicies') {
    scm {
        git {
            remote {
                url('https://github.com/mbaitelman/chef-policy-cookbook.git')
            }
            extensions {
                cleanAfterCheckout()
                relativeTargetDirectory('repo1')
            }
        }
      scriptPath('jenkins/upload-policyies.groovy')
    }
}